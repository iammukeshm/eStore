@using Microsoft.AspNetCore.Identity
@using eStore.Infrastructure.Identity.Models
@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager


@if (SignInManager.IsSignedIn(User))
{
    <li class="nav-item">
        <a class="icons" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">
            <small> @User.Identity.Name </small>
        </a>
    </li>
    <li class="nav-item">
        <a class="icons" asp-area="Identity" asp-page="/Account/Logout" title="Manage">
            <small> logout? </small>
        </a>
    </li>
    @*<li class="nav-item">
        <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Page("/", new { area = "" })" method="post">
           <small><button type="submit" class="nav-link btn btn-link"> logout? </button></small> 
        </form>
    </li>*@
    @*<li class="nav-item">
            <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Hello @User.Identity.Name!</a>
        </li>
        <li class="nav-item">
            <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Page("/", new { area = "" })" method="post">
                <button type="submit" class="nav-link btn btn-link text-dark">Logout</button>
            </form>
        </li>*@
}
else
{
    <li class="nav-item">
        <a class="icons" asp-area="Identity" asp-page="/Account/Login" title="Login">
            <i class="ti-user" aria-hidden="true"></i>
        </a>
    </li>
}
